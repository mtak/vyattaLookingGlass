package LGConfig;
use strict;

sub lg_config {
  my %config;
  #############################################################
  # Edit these values
  $config{'routeServer'} = '';
  $config{'user'} = '';
  $config{'keyFile'} = '';
  #############################################################
  return(%config);
}

1;
